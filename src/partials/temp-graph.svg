<svg class="graph">
  <g id="layer1">
    <path class="strikeout" ng-attr-d="{{ path }}" ng-attr-stroke-dasharray="{{ dash }}">
      <animate attributeName="stroke-dashoffset" ng-attr-from="{{ dash }}" to="0" dur="1s" begin="rect.click;0s"/>
    </path>
  </g>
</svg>
